# Mocha 桌游 / Mocha Tabletop — 项目协作规范

适用于本仓库的全部代码、文案、美术、测试和发布。开始任务先读本文件，再读相关模块和文档。用户当前明确要求优先；遇到要求与实现不一致，记录并修复差距，不能把现有实现或通过的测试当作产品要求本身。本文是长期开发约束，不是“所有要求已经验收通过”的声明。

## 1. 产品背景与边界

- Mocha 桌游是朋友聚会使用的多游戏手机网页 App / PWA，优先照顾 iPhone，同时支持 Android、平板和桌面。打开就能玩，不要求注册、密码或第三方登录；首次选择昵称与头像。
- 品牌统一为「Mocha 桌游」/「Mocha Tabletop」。图标以 Mocha 小狗玩桌游为主题，沿用现有原创图标及风格。不要退回旧项目名，也不要把站点简称成占用其他产品命名空间的单独 `mocha`。
- 当前发布架构是 React + TypeScript + Vite 前端、Cloudflare Pages、Worker + Durable Objects。历史 TestFlight / 原生 App 方案已由网页安装方案替代，不恢复旧工程。
- 保留三种体验：云端联机、经 Cloudflare 配对后 WebRTC 连接房主的局域网联机、可缓存的同屏试玩。局域网发现不等于浏览器扫描 Wi-Fi；直连是否成功取决于网络隔离等条件，不能承诺任意同 Wi-Fi 都能连接。
- 以 Cloudflare 免费方案的成本意识设计：缓存静态资源，减少重复请求和无意义轮询，做好过期清理；新增付费资源、TURN 或其他持续收费依赖前说明影响，不偷偷引入。
- 游戏目录以 `src/core/types.ts`、`src/core/registry.ts` 和实际 UI 为准，不在新增功能时硬编码“只有九款”。新增玩法必须贯通规则、玩家视图、UI、双语、教程、恢复和测试。
- 斗地主、掼蛋、多种麻将是后续已提出的扩展方向；只有合入并验证后才作为现有功能介绍。麻将等地区变体需明确规则集，不能把不同 App / 地方规则拼成一个“标准版”。

背景与历史要求索引见 [需求来源](docs/PRODUCT-REQUIREMENTS.md)。架构见 [ARCHITECTURE](docs/ARCHITECTURE.md)，玩法细节仍需查相应模块、规则来源及测试。

## 2. 规则准确、容易辨认、原创表达

### 玩法忠实度

- 用户要求玩法和数值遵循所选正版规则。改游戏前确认发行方、版本、人数范围、扩展范围和可选桌规；优先查看发行方规则书及勘误，记录链接、版本/查阅日期和对应实现。第三方规则/数据只能辅助交叉核对，不能用印象或同类游戏代替官方依据。
- 逐项核对牌数/牌表、初始资源、行动顺序、合法动作、费用、攻击/否决/质疑时机、同时行动、平局、淘汰、末轮及最终计分。不要为了“更方便”、改名或降低素材相似度偷偷修改数值、删去基础机制或创造近似牌组。
- 没有统一官方版本的传统游戏（例如不同狼人杀桌规、麻将变体）应写明采用的规则集、依据及选项。只实现子集或数字化调整时如实标注，列出差距；没有达到所选范围前不称为“完整官方规则”。现有差异不是永久豁免。
- 教程、开局选项、动作提示、卡牌说明、服务端判定和胜负结算必须一致。简短不能省略决定合法性的数量或条件；如取晶石应说清“三种不同颜色各一枚”及同色两枚的库存条件，而非笼统“不同颜色”。
- 公共信息与秘密信息也属于规则：公开预留与盲抽预留必须区分；宝石牌堆顶可以盲抽预留，不可看牌堆挑牌。UNO / 接龙的 +4 质疑、喊牌、抓漏喊及叠加限制需按选定版本测试，不凭熟悉的民间玩法修改。
- 改规则时用独立来源的期望值和场景测试。卡牌数值 fixture 不能由待测实现自动导出后再验证自身；保留出处及必要授权说明。

### 原创外观与可识别性

- 保留 Mocha 的原创名称、插画、界面和说明表达；不复制官方卡面、包装、Logo、规则书长段文字、专有图标组合或其他项目的代码/素材。公开可下载、开源仓库可见、注明来源或改了名字，都不自动等于可使用。
- 熟悉玩法的人应能从功能名、牌面数值、资源语义、动作顺序和说明认出玩法。优先保留清楚的通用功能词，如“拆弹 / Defuse”“跳过 / Skip”“否决 / Nope”，不要只留下隐喻式新名字让玩家重新猜卡牌作用。
- 原创别名若保留，配清晰功能副标题；同一功能在牌面、教程、日志和中英文中一一对应。必要时在规则/关于页使用准确、克制的文字说明参考玩法与版本，避免官方标识、授权暗示和误导性品牌呈现。
- 建立或更新相应玩法的「内部 ID → 中文展示 → 英文展示 → 原规则功能 → 规则来源」对照；内部 ID 不因改展示名随意变化，以免破坏保存的牌局。
- 不用“玩法不受保护”“改名就不侵权”等绝对结论为素材使用背书。有不明来源、许可限制或过度相似的表达，替换为自创/明确授权内容并记录问题。不能通过擅自改玩法来回避表达层面的风险。

## 3. 完整的中文 / English 体验

- 中英是同等完整的产品体验：首页、首次昵称、头像、安装、建房/加入/审批、规则选项、大厅、牌桌、所有弹窗、终局、战绩、恢复、错误、断线提示、aria-label、alt 和动态日志都检查。
- 语言入口使用目标语言的原生名称「中文 / English」，包括首次进入弹窗。切换后保存偏好；两种入口都能找到，不让不懂当前语言的人猜按钮。
- 优先使用 `src/i18n.ts` 与 `src/i18n/` 现有机制，避免在各组件复制词典或用全局字符串替换翻译。新增键/动态分支同时完成两种语言；检查占位符、数量、单位、复数、角色、玩家名称插值和长文本。
- 区分系统文案与用户昵称/词面等内容，不能翻译用户输入。词语游戏的词库语言独立于界面语言；中途切换界面语言不得换掉已发词语、泄露密钥或重置对局。
- 两种语言的术语与数值条件相同，译文自然、简洁。英文更长也必须可读；不得靠缩小到难以阅读、裁掉关键动作词或隐藏内容解决布局。
- 检查 zh → en → zh、刷新后的语言记忆、游戏中切换、无障碍名称及出错分支。英文界面扫描残留中文只是辅助，不足以证明语义、玩法条件或全部分支正确。

## 4. UI 与交互质量

- 简单、精美、像经过设计的桌游；沿用现有插画、配色、层次、圆角和字体体系。不要只用通用后台表格、满屏文字或一堆 Emoji 代替牌桌美术。
- 首页紧凑，突出游戏选择和开桌。删除无助于操作的营销口号、重复说明和机械文案；不要添加“今晚，玩点什么”“先选牌，再出牌”等用户已明确不喜欢的提示。必要规则用准确短句、图示或按需帮助表达。
- **手机竖屏与短横屏必须都好用，允许采用不同布局。** 不能以“请旋转屏幕”替代设计，也不能只是把桌面等比例缩小。
- UI 改动至少检查 320×568、390×844、430×932、844×390、932×430、768×1024、1440×900；这是 CSS 像素验收起点，增加改动相关尺寸。用中文和英文检查，并在牌局/弹窗打开时旋转。
- 处理安全区、动态视口高度、浏览器工具栏、软键盘、长昵称、英文长标签、最大人数/手牌/数字、长日志和空状态。页面不产生意外水平溢出；手牌区域可有明确可发现的横向滚动，但不遮挡必须看到的费用、行动或确认按钮。
- 手牌、公共市场、库存、回合状态、选中状态和主动作有清楚层级；关键数值、图标、费用、公开预留和已购发展卡信息能看清，缩略卡也不能把数字挤出边框。
- 卡牌选择以真实交互表达：点选高亮、适度抬升/阴影、再次点击取消；有效选择后点亮确认/出牌按钮。万能牌先选颜色。换座、换回合、状态更新或重开后清理失效选中，不允许重复提交。
- 晶石通过直接点选取用，界面不强迫用户先选“同色/不同色”模式。非法动作提供可理解原因；等待时显示轮到谁和当前阶段，避免每张卡重复长段不可用提示。
- 身份大图应完整显示当前角色；图集不能漏出相邻角色。默认收起秘密，查看后可方便关闭；换座、重发身份、重开后重新隐藏。最大人数时也能查看所有座位及必要信息。
- 操作区域以约 44×44 CSS 像素触控目标为设计基准；颜色之外提供形状/文字/选中语义。支持键盘、可见焦点、语义化按钮和 reduced-motion。弹窗管理焦点、Escape、关闭与返回焦点；嵌套弹窗关闭后不能操作错误层。
- 用实际点击、完整回合和截图检查美感与可用性；“没有溢出”“截图能生成”或测试只检查元素存在，不等于 UI 好用。自动化不能证明真机 iPhone 安装体验时，要清楚说明未验证范围。

## 5. ImageGen 与素材规范

- 重要封面、身份、卡牌场景、桌面背景和品牌图标使用高质量原创美术；新增或替换此类位图时使用可用的 ImageGen / Imagen 图像生成能力，沿用现有风格。纯文档改动不需要无意义地重画素材。
- 图像提示明确要求无文字、无字母、无数字、无商标和水印；牌名、费用、分值、资源数量与规则文字由 HTML / SVG 和真实数据清晰绘制，不依赖生成图片中的字。
- 生成完成必须查看原图与实际裁切后的页面：剔除伪文字、乱码、奇怪字形、多余肢体、不合逻辑的器物、穿帮图集和低质拼贴。用户所说“不要 AI 味”应落实到逐图验收，不能仅在 prompt 写一句。
- 图集记录行列、顺序、比例和安全边距；同步 `Art.tsx`、`IllustratedTile.tsx` 和相关 CSS。大图采用适合内容的 contain/meet，小缩略图可合理裁切；任何裁切都不能串格或泄露其他身份。
- 普通 UI 图标复用 `lucide-react`，几何牌面、晶石、精确点数可用代码/SVG。不要为简单图标生成大位图，也不要用粗糙代码图取代重要插画。
- 压缩部署素材，检查不同像素密度与移动端解码/加载；记录新增体积并避免不必要的高分辨率原图进入 `public/`。修改图标同步 PWA / Apple touch icon 所需尺寸。
- 维护 [ASSETS](docs/ASSETS.md)：来源、生成方式/提示文件、用途、图集映射、许可/署名要求。私人参考照片、带签名的临时下载链接、账号资料和生成过程日志不提交到公共仓库。

## 6. 联机、隐私与边界情况

- `GameModule.create/apply/view` 是规则入口；UI 展示过滤后的视图和可用动作，不另外实现一份规则。`apply` 校验阶段、身份、人数、选项、资源与指令；非法指令不得部分修改原状态，状态须可序列化。
- 云端以 Durable Object 的权威状态为准。只向对应玩家发送其合法视图；牌堆、对手手牌、秘密角色、词语密钥和私人质疑证据不能先发给所有人再靠 CSS 隐藏。日志、错误、DOM、调试输出和缓存也不能泄露。
- 局域网由房主持有完整状态，房主属于信任边界；不要宣传为连房主也无法窥视。同屏试玩是换座学习工具，不能宣称提供联机级隐私。
- 房间码不是玩家身份凭据；保留房间凭据、邀请/房主审批机制及来源校验。新加入、重连、过期、冒用玩家 ID、非法命令、重复消息及过量输入都在可信端校验。
- 创建、邀请、审批、拒绝、取消加入、满员、准备、开局、重开、退出和解散都有明确可恢复状态。更改玩法选项后重新准备；访客能查看规则但不能更改房主设置。
- 掉线保留座位并暂停需要玩家参与的对局；重连恢复原座位和当前状态，不能自动替玩家出牌或换成新身份。房主退出、无法重连、连接超时、WebRTC 失败要有明确解释和可用退出/改用云端路径，不在后台悄悄重置对局。
- 处理旧连接迟到消息、重复连接、后台切换、刷新、浏览器关闭、房主恢复及局域网配对失败。涉及网络变动时分别测试云端和真实 LAN；WebKit 的 ICE 失败不能记作 LAN 已通过。

## 7. 本地数据与 PWA

- 保留昵称与丰富头像选择，刷新无需重填。沿用 `src/local/storage.ts` 的存储封装，处理禁用存储、配额、损坏数据和版本升级；失败显示可理解提示，不白屏。
- 战绩仅保存本人已接收的终局摘要，以持久 matchID 去重；支持单条删除、全部删除，删除后刷新终局不能重新出现。同屏/法官局明确标记，不冒充个人联机胜负；不存对手秘密数据。
- 同一浏览器关闭/误划出后可恢复未过期会话；主动退出、解散、过期和重置后清理正确凭据，不恢复已经结束的旧房间。旧存档变更要兼容读取或提供清楚迁移/失效提示，不静默丢弃用户数据。
- 重置本机身份与战绩属于破坏性用户操作：先离开牌桌，在 UI 内确认后再清除。明确存储不跨设备/域名同步，PWA 与 Safari 的存储也不应假定相通。
- 支持原生安装提示的浏览器使用系统安装入口；iPhone 提供准确的分享菜单指引，不能伪装成网页可以自动安装。安装成功、取消、已安装和不支持状态都检查。
- 通过 `scripts/build-sw.mjs` 生成内容哈希缓存清单，不手改 `dist/` 或缓存列表；验证首次缓存、版本更新、断网重载和试玩素材。API/房间凭据/秘密视图不得进入公共离线缓存。

## 8. 代码与仓库维护

- Node.js 22+，npm，使用现有 lockfile 和项目本地工具。默认 `npm ci`，不顺手升级框架/依赖或改变部署架构；确有需要才更新并解释原因。
- 先查 `rg`、读相关类型/实现/测试再编辑。规则放 `src/core/`，网络放 `src/net/` / `worker/`，界面放 `src/ui/`，翻译放 `src/i18n/`，持久化放 `src/local/`。保持模块边界，不把整套新游戏塞进 App 主组件。
- 新增代码尽量使用明确类型、可读命名和小函数，不扩大现有 `any`、禁用类型检查、复制大块逻辑或引入未使用抽象。输入验证在边界执行；注释说明规则依据和原因，不重复代码字面意思。
- 随机逻辑沿用可复现 seed / shuffle，网络身份凭据不能用游戏随机数代替。验证无效动作不变状态、卡牌/资源守恒、边界回合和终局。
- 保留其他工作副本/分支的未提交内容。独立任务使用独立 worktree 和分支，从最新 `origin/main` 建立；不要在别人正在用的 checkout 切分支、清理目录或强推共享 main。
- 有用户授权时可用子代理实现/试玩/批判独立模块；分清文件所有权，主执行者仍须集成审查。历史某次任务使用过子代理不等于以后所有任务都必须启动子代理。
- 仓库只保留必要源码、文档、可用美术与独立测试数据。不得提交 `node_modules/`、`dist/`、`.wrangler/`、截图/报告、凭据、私人照片或失效旧参考。清理历史参考需先确认无运行、来源追踪或测试用途。
- README 面向玩家和维护者，准确写品牌、功能、启动、测试和部署；不堆临时汇报。架构、规则、素材、测试命令变化同步相应文档。`AGENTS.md` 维护长期规则，不积累每次部署流水账。

## 9. 验证与完成标准

先按变更范围选择证据，不为文案修改制造与实现一模一样的测试；也不能用一个烟雾测试代表所有游戏、语言或人数。

| 变更 | 必须检查的证据 |
| --- | --- |
| 所有提交 | diff 审查、文档/链接/命令准确性、`git diff --check`、`npm run check` |
| 规则/牌表 | 独立规则依据、相关规则单测、非法动作原子性、隐藏视图、边界与实际终局；UI/教程与结果一致 |
| 文案/双语 | 两语言全部受影响状态、占位符与条件对应、动态日志/错误/aria、切换与持久记忆 |
| 布局/美术/交互 | 本文尺寸矩阵与两语言、真实点击、最大数据量、弹窗/旋转、截图人工查看、相关浏览器脚本 |
| 网络/持久化 | 相关 `tests/network/`、多浏览器上下文、加入/重连/退出/解散、服务端恢复、权限与秘密信息 |
| PWA/缓存 | 生产构建上的安装状态、缓存更新、离线试玩、主屏图标；真机验证与自动化结论分开 |
| 发布 | 目标 SHA / CI、Worker 打包预检、远端部署记录、两正式域名与 API、生产联机回归 |

- 标准门禁：`npm run check`（Vitest、Worker 类型检查、前端类型检查及生产构建）。GitHub Actions 的实际覆盖见 `.github/workflows/ci.yml`，不要把绿色 CI 称作所有平台完整验收。
- UI、网络与生产脚本命令见 [tests/README.md](tests/README.md)，执行前读脚本所需服务和环境变量。常用：`language`、`i18n`、`responsive`、`identity-portraits`、`uno-selection`、`personal`、`production` 的 `.integration.mjs`。
- 默认本地 Worker 8787、前端 5174；端口被占用时先确认所有者，使用自己的端口/inspector、匹配的 API 地址和允许来源，不停止他人的服务。
- 失败应定位根因并修复；不能删断言、跳过失败路径、增加无限等待或降低规则准确性来获得绿色结果。对基线故障、环境限制和未执行检查明确记录，不报成通过。
- “完成”需要逐项对照用户要求和证据。检查功能之外，还主动发现错译、误导规则、糟糕提示、泄密、操作卡死和不美观等问题；只运行现成脚本不替代试玩审查。

## 10. 推送、合并与 Cloudflare 发布

- 按本次用户授权范围行动。用户已明确要求 push、merge、deploy 时完成验证后直接执行，不重复索要同一批准；用户只要求本地修改时，不把本文当作发布授权。
- 发布步骤：独立分支完成改动 → 本地验证 → 推送 / PR → 检查 PR CI 与最终 diff → 合并 main → 获取最终 main SHA → 构建/验证该版本 → 更新生产 Worker 与 Pages → 线上验证。不要从落后分支发布覆盖其他已合并成果。
- 多任务并行时，合并前和发布前重新 `git fetch origin` 并检查分支/部署记录；如 main 已变化，合入新 main 并检查受影响范围。不要绕过失败 CI 或强推 main。只在本地主 main 无未提交改动且可快进时同步该副本。
- 目标配置以 `wrangler.jsonc`、`.env.production`、`package.json` 与 [DEPLOYMENT](docs/DEPLOYMENT.md) 为准：Worker `mocha-tabletop`，Pages `mocha-tabletop-web`，正式入口 `https://mocha-tabletop-web.pages.dev/` 与 `https://mocha-tabletop.bianhengwei.com/`。不创建同名重复项目，不复活已废弃项目。
- 使用 lockfile 中的 Wrangler，先查看所用命令帮助并核对登录账户、绑定、来源与远端配置。`npx wrangler deploy --dry-run` 仅证明打包，不证明线上可用；保留已应用的 Durable Objects migration。
- `npm run deploy` 依次构建并发布 Worker 与 Pages；也可在同一已验证构建上执行对应 Wrangler 命令，并记录完整 Git SHA、Worker 版本与 Pages deployment。发布前核对 dashboard 差异，不能无意覆盖远端变量/路由。
- GitHub CI 目前只做验证，不自动部署。合并 PR 不代表 Cloudflare 已更新，CLI 成功也不代表正式域名已经加载新版本。
- 发布后核对两个正式入口、Worker 健康与允许来源、HTML/资源/Service Worker 的版本一致性，并运行生产多人房间/恢复/离线回归。只创建可识别的测试房间，结束后退出/解散；不修改真实玩家牌局。
- 最终交付简述改动、独立目录/分支、PR/合并 SHA、部署地址、实际通过的检查和剩余限制。若有一步未完成，就明确说未完成，不能用“已上线”概括部分发布。
