{
  "name": "mocha-tabletop-web",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build && node scripts/build-sw.mjs",
    "test": "vitest run",
    "preview": "vite preview --host 127.0.0.1",
    "worker:dev": "wrangler dev",
    "deploy": "npm run deploy:worker && npm run deploy:web",
    "cf:types": "wrangler types",
    "deploy:worker": "npm run build && wrangler deploy",
    "deploy:web": "npm run build && wrangler pages deploy dist --project-name mocha-tabletop-web --branch main",
    "typecheck:worker": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022 --types @cloudflare/workers-types --skipLibCheck worker/index.ts",
    "check": "npm test && npm run typecheck:worker && npm run build"
  },
  "dependencies": {
    "lucide-react": "0.468.0",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260919.1",
    "@playwright/test": "1.55.1",
    "@types/node": "24.13.6",
    "@types/react": "19.1.16",
    "@types/react-dom": "19.1.9",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "4.1.11",
    "wrangler": "4.135.0"
  }
}
