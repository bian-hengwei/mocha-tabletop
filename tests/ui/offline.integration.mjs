import {chromium} from '@playwright/test';import assert from 'node:assert/strict';
const browser=await chromium.launch({executablePath:process.env.CHROME_PATH||undefined});
try{const context=await browser.newContext({viewport:{width:390,height:844}});const page=await context.newPage();page.on('dialog',d=>d.accept());const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(process.env.BASE_URL||'http://127.0.0.1:5176');await page.evaluate(()=>{localStorage.setItem('mocha-profile',JSON.stringify({id:'offline-test-1',name:'Offline',avatar:'🐶'}));});await page.evaluate(()=>navigator.serviceWorker.ready);await page.waitForFunction(async()=>{for(const key of await caches.keys()){if(key.startsWith('mocha-')&&(await(await caches.open(key)).keys()).length>=20)return true;}return false;});await context.setOffline(true);await page.reload();await page.locator('.game-cover').first().waitFor();assert.equal(await page.locator('.game-cover').count(),9);for(const kind of ['gems','bombs','werewolf','avalon','sushi','century','uno','codenames','undercover']){await page.locator('.cover-'+kind).click();await page.getByRole('button',{name:'同屏试玩',exact:true}).click();await page.locator('.game-surface').waitFor();await page.getByRole('button',{name:'牌桌菜单',exact:true}).click();await page.getByRole('button',{name:'结束试玩',exact:true}).click();}assert.equal(await page.locator('.game-cover img').evaluateAll(imgs=>imgs.every(i=>i.complete&&i.naturalWidth>0)),true);assert.deepEqual(errors,[]);console.log('PASS production cache, offline reload, nine practices, all cover images');}finally{await browser.close();}
